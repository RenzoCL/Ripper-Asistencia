# requirements.txt — Servidor
# FIX: opencv-python-headless reemplazado por opencv-contrib-python
# (cv2.face para LBPH solo existe en contrib)

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
sqlalchemy==2.0.36
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# FIX CRÍTICO: era opencv-python-headless — no incluye cv2.face (LBPH)
# Usar opencv-contrib-python en su lugar
opencv-contrib-python==4.10.0.84

httpx==0.28.1
aiofiles==24.1.0
openpyxl==3.1.5
reportlab==4.2.5
setuptools==69.0.3
apscheduler==3.10.4
pytz==2024.2
numpy==1.26.4
Pillow==11.0.0

# NUEVO: rate limiting para /scan y endpoints críticos
slowapi==0.1.9

# NUEVO: para paginación eficiente en listados grandes
# (ya incluido en fastapi, pero explícito para claridad)